error in line# 2
     lexed as:  inherits IO {
    should be:  TYPEID Main
error in line# 3
     lexed as:     main() : SELF_TYPE {
    should be:  INHERITS
error in line# 4
     lexed as:  c : Complex <- (new Complex).init(1, 1) in
    should be:  TYPEID IO
error in line# 5
     lexed as:     {
    should be:  '{'
error in line# 6
     lexed as:         -- trivially equal (see CoolAid)
    should be:  OBJECTID main
error in line# 7
     lexed as:         if c.reflect_X() = c.reflect_0()
    should be:  '('
error in line# 8
     lexed as:         then out_string("=)\n")
    should be:  ')'
error in line# 9
     lexed as:         #8 ELSE
    should be:  ':'
error in line# 10
     lexed as:  out_string("=(\n")
    should be:  TYPEID SELF_TYPE
error in line# 11
     lexed as:         fi;
    should be:  '{'
error in line# 12
     lexed as:  equal
    should be:  '('
error in line# 13
     lexed as:         if c.reflect_X().reflect_Y().equal(c.reflect_0())
    should be:  LET
error in line# 14
     lexed as:         then out_string("=)\n")
    should be:  OBJECTID c
error in line# 15
     lexed as:         #13 ELSE
    should be:  ':'
error in line# 16
     lexed as:  out_string("=(\n")
    should be:  TYPEID Complex
error in line# 17
     lexed as:         fi;
    should be:  ASSIGN
error in line# 18
     lexed as:     }
    should be:  '('
error in line# 19
     lexed as: 	)
    should be:  NEW
error in line# 20
     lexed as:     };
    should be:  TYPEID Complex
error in line# 21
     lexed as: };
    should be:  ')'
error in line# 22
     lexed as: 
    should be:  '.'
error in line# 23
     lexed as:  CLASS
    should be:  OBJECTID init
error in line# 24
     lexed as:  inherits IO {
    should be:  '('
error in line# 25
     lexed as:     x : Int;
    should be:  INT_CONST 1
error in line# 26
     lexed as:     y : Int;
    should be:  ','
error in line# 27
     lexed as: 
    should be:  INT_CONST 1
error in line# 28
     lexed as:     init(a : Int, b : Int) : Complex {
    should be:  ')'
error in line# 29
     lexed as: 	{
    should be:  IN
error in line# 30
     lexed as:     x = a;
    should be:  '{'
error in line# 31
     lexed as:     y = b;
    should be:  IF
error in line# 32
     lexed as:     self;
    should be:  OBJECTID c
error in line# 33
     lexed as: 	}
    should be:  '.'
error in line# 34
     lexed as:     };
    should be:  OBJECTID reflect_X
error in line# 35
     lexed as: 
    should be:  '('
error in line# 36
     lexed as:     print() : Object {
    should be:  ')'
error in line# 37
     lexed as:  y = 0
    should be:  '='
error in line# 38
     lexed as:  out_int(x)
    should be:  OBJECTID c
error in line# 39
     lexed as:  ELSE
    should be:  '.'
error in line# 40
     lexed as:  out_int(x).out_string("+").out_int(y).out_string("I")
    should be:  OBJECTID reflect_0
error in line# 41
     lexed as: 	fi
    should be:  '('
error in line# 42
     lexed as:     };
    should be:  ')'
error in line# 43
     lexed as: 
    should be:  THEN
error in line# 44
     lexed as:     reflect_0() : Complex {
    should be:  OBJECTID out_string
error in line# 45
     lexed as: 	{
    should be:  '('
error in line# 46
     lexed as:     x = ~x;
    should be:  STR_CONST "=)\n"
error in line# 47
     lexed as:     y = ~y;
    should be:  ')'
error in line# 48
     lexed as:     self;
    should be:  ELSE
error in line# 49
     lexed as: 	}
    should be:  OBJECTID out_string
error in line# 50
     lexed as:     };
    should be:  '('
error in line# 51
     lexed as: 
    should be:  STR_CONST "=(\n"
error in line# 52
     lexed as:     reflect_X() : Complex {
    should be:  ')'
error in line# 53
     lexed as: 	{
    should be:  FI
error in line# 54
     lexed as:     y = ~y;
    should be:  ';'
error in line# 55
     lexed as:     self;
    should be:  IF
error in line# 56
     lexed as: 	}
    should be:  OBJECTID c
error in line# 57
     lexed as:     };
    should be:  '.'
error in line# 58
     lexed as: 
    should be:  OBJECTID reflect_X
error in line# 59
     lexed as:     reflect_Y() : Complex {
    should be:  '('
error in line# 60
     lexed as: 	{
    should be:  ')'
error in line# 61
     lexed as:     x = ~x;
    should be:  '.'
error in line# 62
     lexed as:     self;
    should be:  OBJECTID reflect_Y
error in line# 63
     lexed as: 	}
    should be:  '('
error in line# 64
     lexed as:     };
    should be:  ')'
error in line# 65
     lexed as: 
    should be:  '.'
error in line# 66
     lexed as:     equal(d : Complex) : Bool {
    should be:  OBJECTID equal
error in line# 67
     lexed as:  x = d.x_value()
    should be:  '('
error in line# 68
     lexed as: 	then
    should be:  OBJECTID c
error in line# 69
     lexed as:     if y = d.y_value()
    should be:  '.'
error in line# 70
     lexed as:     then true
    should be:  OBJECTID reflect_0
error in line# 71
     lexed as:     #66 ELSE
    should be:  '('
error in line# 72
     lexed as:  false
    should be:  ')'
error in line# 73
     lexed as:     fi
    should be:  ')'
error in line# 74
     lexed as:  ELSE
    should be:  THEN
error in line# 75
     lexed as:  false
    should be:  OBJECTID out_string
error in line# 76
     lexed as: 	fi
    should be:  '('
error in line# 77
     lexed as:     };
    should be:  STR_CONST "=)\n"
error in line# 78
     lexed as: 
    should be:  ')'
error in line# 79
     lexed as:     x_value() : Int {
    should be:  ELSE
error in line# 80
     lexed as: 	x
    should be:  OBJECTID out_string
error in line# 81
     lexed as:     };
    should be:  '('
error in line# 82
     lexed as: 
    should be:  STR_CONST "=(\n"
error in line# 83
     lexed as:     y_value() : Int {
    should be:  ')'
error in line# 84
     lexed as: 	y
    should be:  FI
error in line# 85
     lexed as:     };
    should be:  ';'
error in line# 86
     lexed as: };
    should be:  '}'
