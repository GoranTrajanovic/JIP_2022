error in line# 1
     lexed as: 
    should be:  CLASS
error in line# 2
     lexed as: 
    should be:  TYPEID A2I
error in line# 3
     lexed as: 
    should be:  '{'
error in line# 4
     lexed as: 
    should be:  OBJECTID c2i
error in line# 5
     lexed as: 
    should be:  '('
error in line# 6
     lexed as: 
    should be:  OBJECTID char
error in line# 7
     lexed as: 
    should be:  ':'
error in line# 8
     lexed as: 
    should be:  TYPEID String
error in line# 9
     lexed as: 
    should be:  ')'
error in line# 10
     lexed as: 
    should be:  ':'
error in line# 11
     lexed as: 
    should be:  TYPEID Int
error in line# 12
     lexed as: 
    should be:  '{'
error in line# 13
     lexed as:  CLASS
    should be:  IF
error in line# 14
     lexed as:  {
    should be:  OBJECTID char
error in line# 15
     lexed as: 
    should be:  '='
error in line# 16
     lexed as:      c2i(char : String) : Int {
    should be:  STR_CONST "0"
error in line# 17
     lexed as:  char = "0" then 0 #16 ELSE
    should be:  THEN
error in line# 18
     lexed as: 
    should be:  INT_CONST 0
error in line# 19
     lexed as:  char = "1" then 1 #17 ELSE
    should be:  ELSE
error in line# 20
     lexed as: 
    should be:  IF
error in line# 21
     lexed as:  char = "2" then 2 #18 ELSE
    should be:  OBJECTID char
error in line# 22
     lexed as: 
    should be:  '='
error in line# 23
     lexed as:         if char = "3" then 3 #19 ELSE
    should be:  STR_CONST "1"
error in line# 24
     lexed as: 
    should be:  THEN
error in line# 25
     lexed as:         if char = "4" then 4 #20 ELSE
    should be:  INT_CONST 1
error in line# 26
     lexed as: 
    should be:  ELSE
error in line# 27
     lexed as:         if char = "5" then 5 #21 ELSE
    should be:  IF
error in line# 28
     lexed as: 
    should be:  OBJECTID char
error in line# 29
     lexed as:         if char = "6" then 6 #22 ELSE
    should be:  '='
error in line# 30
     lexed as: 
    should be:  STR_CONST "2"
error in line# 31
     lexed as:         if char = "7" then 7 #23 ELSE
    should be:  THEN
error in line# 32
     lexed as: 
    should be:  INT_CONST 2
error in line# 33
     lexed as:         if char = "8" then 8 #24 ELSE
    should be:  ELSE
error in line# 34
     lexed as: 
    should be:  IF
error in line# 35
     lexed as:         if char = "9" then 9 #25 ELSE
    should be:  OBJECTID char
error in line# 36
     lexed as: 
    should be:  '='
error in line# 37
     lexed as:         { abort(); 0; }  -- the 0 is needed to satisfy the typchecker
    should be:  STR_CONST "3"
error in line# 38
     lexed as:         fi fi fi fi fi fi fi fi fi fi
    should be:  THEN
error in line# 39
     lexed as:      };
    should be:  INT_CONST 3
error in line# 40
     lexed as: 
    should be:  ELSE
error in line# 41
     lexed as: 
    should be:  IF
error in line# 42
     lexed as: 
    should be:  OBJECTID char
error in line# 43
     lexed as: 
    should be:  '='
error in line# 44
     lexed as:      i2c(i : Int) : String {
    should be:  STR_CONST "4"
error in line# 45
     lexed as:  i = 0 then "0" #34 ELSE
    should be:  THEN
error in line# 46
     lexed as: 
    should be:  INT_CONST 4
error in line# 47
     lexed as:  i = 1 then "1" #35 ELSE
    should be:  ELSE
error in line# 48
     lexed as: 
    should be:  IF
error in line# 49
     lexed as:  i = 2 then "2" #36 ELSE
    should be:  OBJECTID char
error in line# 50
     lexed as: 
    should be:  '='
error in line# 51
     lexed as:  i = 3 then "3" #37 ELSE
    should be:  STR_CONST "5"
error in line# 52
     lexed as: 
    should be:  THEN
error in line# 53
     lexed as:  i = 4 then "4" #38 ELSE
    should be:  INT_CONST 5
error in line# 54
     lexed as: 
    should be:  ELSE
error in line# 55
     lexed as:  i = 5 then "5" #39 ELSE
    should be:  IF
error in line# 56
     lexed as: 
    should be:  OBJECTID char
error in line# 57
     lexed as:  i = 6 then "6" #40 ELSE
    should be:  '='
error in line# 58
     lexed as: 
    should be:  STR_CONST "6"
error in line# 59
     lexed as:  i = 7 then "7" #41 ELSE
    should be:  THEN
error in line# 60
     lexed as: 
    should be:  INT_CONST 6
error in line# 61
     lexed as:  i = 8 then "8" #42 ELSE
    should be:  ELSE
error in line# 62
     lexed as: 
    should be:  IF
error in line# 63
     lexed as:  i = 9 then "9" #43 ELSE
    should be:  OBJECTID char
error in line# 64
     lexed as: 
    should be:  '='
error in line# 65
     lexed as:  abort(); ""; }  -- the "" is needed to satisfy the typchecker
    should be:  STR_CONST "7"
error in line# 66
     lexed as:         fi fi fi fi fi fi fi fi fi fi
    should be:  THEN
error in line# 67
     lexed as:      };
    should be:  INT_CONST 7
error in line# 68
     lexed as: 
    should be:  ELSE
error in line# 69
     lexed as: 
    should be:  IF
error in line# 70
     lexed as: 
    should be:  OBJECTID char
error in line# 71
     lexed as: 
    should be:  '='
error in line# 72
     lexed as: 
    should be:  STR_CONST "8"
error in line# 73
     lexed as: 
    should be:  THEN
error in line# 74
     lexed as: 
    should be:  INT_CONST 8
error in line# 75
     lexed as: 
    should be:  ELSE
error in line# 76
     lexed as: 
    should be:  IF
error in line# 77
     lexed as:      a2i(s : String) : Int {
    should be:  OBJECTID char
error in line# 78
     lexed as:         if s.length() = 0 then 0 #57 ELSE
    should be:  '='
error in line# 79
     lexed as: 
    should be:  STR_CONST "9"
error in line# 80
     lexed as:  s.substr(0,1) = "-" then ~a2i_aux(s.substr(1,s.length()-1)) #58 ELSE
    should be:  THEN
error in line# 81
     lexed as: 
    should be:  INT_CONST 9
error in line# 82
     lexed as:         if s.substr(0,1) = "+" then a2i_aux(s.substr(1,s.length()-1)) #59 ELSE
    should be:  ELSE
error in line# 83
     lexed as: 
    should be:  '{'
error in line# 84
     lexed as:            a2i_aux(s)
    should be:  OBJECTID abort
error in line# 85
     lexed as:         fi fi fi
    should be:  '('
error in line# 86
     lexed as:      };
    should be:  ')'
error in line# 87
     lexed as: 
    should be:  ';'
error in line# 88
     lexed as: 
    should be:  INT_CONST 0
error in line# 89
     lexed as: 
    should be:  ';'
error in line# 90
     lexed as: 
    should be:  '}'
error in line# 91
     lexed as: 
    should be:  FI
error in line# 92
     lexed as:      a2i_aux(s : String) : Int {
    should be:  FI
error in line# 93
     lexed as:  int : Int <- 0 in	
    should be:  FI
error in line# 94
     lexed as:            {	
    should be:  FI
error in line# 95
     lexed as:                (let j : Int <- s.length() in
    should be:  FI
error in line# 96
     lexed as:           (let i : Int <- 0 in
    should be:  FI
error in line# 97
     lexed as:     while i < j loop
    should be:  FI
error in line# 98
     lexed as: 			{
    should be:  FI
error in line# 99
     lexed as:     int <- int * 10 + c2i(s.substr(i,1));
    should be:  FI
error in line# 100
     lexed as:     i <- i + 1;
    should be:  FI
error in line# 101
     lexed as: 			}
    should be:  '}'
error in line# 102
     lexed as:     pool
    should be:  ';'
error in line# 103
     lexed as:   )
    should be:  OBJECTID i2c
error in line# 104
     lexed as:        );
    should be:  '('
error in line# 105
     lexed as:               int;
    should be:  OBJECTID i
error in line# 106
     lexed as:     }
    should be:  ':'
error in line# 107
     lexed as:         )
    should be:  TYPEID Int
error in line# 108
     lexed as:      };
    should be:  ')'
error in line# 109
     lexed as: 
    should be:  ':'
error in line# 110
     lexed as: 
    should be:  TYPEID String
error in line# 111
     lexed as: 
    should be:  '{'
error in line# 112
     lexed as: 
    should be:  IF
error in line# 113
     lexed as: 
    should be:  OBJECTID i
error in line# 114
     lexed as:     i2a(i : Int) : String {
    should be:  '='
error in line# 115
     lexed as:  i = 0 then "0" #91 ELSE
    should be:  INT_CONST 0
error in line# 116
     lexed as:  
    should be:  THEN
error in line# 117
     lexed as:         if 0 < i then i2a_aux(i) #92 ELSE
    should be:  STR_CONST "0"
error in line# 118
     lexed as: 
    should be:  ELSE
error in line# 119
     lexed as:           "-".concat(i2a_aux(i * ~1)) 
    should be:  IF
error in line# 120
     lexed as:         fi fi
    should be:  OBJECTID i
error in line# 121
     lexed as:     };
    should be:  '='
error in line# 122
     lexed as: 	
    should be:  INT_CONST 1
error in line# 123
     lexed as: 
    should be:  THEN
error in line# 124
     lexed as: 
    should be:  STR_CONST "1"
error in line# 125
     lexed as: 		
    should be:  ELSE
error in line# 126
     lexed as:     i2a_aux(i : Int) : String {
    should be:  IF
error in line# 127
     lexed as:         if i = 0 then "" #101 ELSE
    should be:  OBJECTID i
error in line# 128
     lexed as:  
    should be:  '='
error in line# 129
     lexed as:     (let next : Int <- i / 10 in
    should be:  INT_CONST 2
error in line# 130
     lexed as:  - next * 10))
    should be:  THEN
error in line# 131
     lexed as:     )
    should be:  STR_CONST "2"
error in line# 132
     lexed as:         fi
    should be:  ELSE
error in line# 133
     lexed as:     };
    should be:  IF
error in line# 134
     lexed as: 
    should be:  OBJECTID i
error in line# 135
     lexed as: };
    should be:  '='
