error in line# 1
     lexed as:  is a string" "so is this"
    should be:  STR_CONST "this is a string"
error in line# 2
     lexed as:  more string"
    should be:  STR_CONST "so is this"
error in line# 3
     lexed as: 
    should be:  STR_CONST "one more string"
error in line# 4
     lexed as:  can have keywords inside: if #4 ELSE
    should be:  STR_CONST "strings can have keywords inside: if else case"
error in line# 5
     lexed as:  case"
    should be:  STR_CONST "!@#$%^&*()_+1234567890-=\tjustanother string   "
error in line# 6
     lexed as:  string   "
    should be: 
